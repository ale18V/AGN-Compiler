// Example file to demonstrate function notation

func add(int x, int y) {
    return x + y;
}

func mul(int x, int y) {
     return x * y;
}

func main() {
    int x;
    int y;
    x = add(10, 2);
    write(x);
    y = mul(x, x + y);
    write(y);
}
