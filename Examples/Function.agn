// Example file to demonstrate function notation

define add as (int x, int y) -> int {
    return x + y;
}

define mul as (int x, int y) -> int {
     return x * y;
}

define main as() -> {
    int x;
    int y;
    x = add(10, 2);
    write(x);
    y = mul(x, x + y);
    write(y);
}
