// Example program demonstrating a nested loop

define main as() -> {
    int x;
    int y;
    x = 0;
    while x < 2 {
        y = 0;
        while y < 3 {
            write(y);
            y = y + 1;
        }
        x = x + 1;
    }
}
